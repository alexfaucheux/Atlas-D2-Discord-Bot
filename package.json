{
  "name": "sample-discord-bot",
  "version": "1.0.0",
  "description": "first bot",
  "main": "index.js",
  "scripts": {
    "start": "npm run deploy-run",
    "deploy-global": "node deploy-commands.js -global -live",
    "deploy-global-run": "node deploy-commands.js -global -live && node start-bot.js",
    "deploy-run": "node deploy-commands.js && node start-bot.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.2.0-alpha.1",
    "date-and-time": "^3.0.0",
    "dateformat": "^5.0.3",
    "discord.js": "^14.10.2",
    "dotenv": "^16.0.3",
    "fs": "^0.0.1-security",
    "mongoose": "^7.1.0"
  },
  "devDependencies": {
    "eslint": "^8.39.0"
  }
}
